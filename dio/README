# dio

diamond-io cli implementation

### install
```
cargo install --path .
```

disk feature enable
```
cargo install --features disk --path .
```


debug feature enable
```
cargo install --features debug --path .
```

### run 
```
dio run-bench -c {CONFIG-TOML-PATH} -o {OBFUSCATION-DIRECTORY-PATH} --add-num {ADD-GATE-NUMBER} --mul-num {MUL-GATE-NUMBER}
```